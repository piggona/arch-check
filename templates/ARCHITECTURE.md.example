# ARCHITECTURE.md — 项目架构契约模板

<!-- 把本文件复制到你的项目根目录并按实际情况修改。arch-check 插件会把它
     作为最高规则来源：/arch-check、/arch-audit 与 @arch-reviewer 都按它执行。 -->

## 分层定义

| 层 | 目录 | 职责 | 禁止 |
|---|---|---|---|
| UI | `src/ui/` | 页面、组件、展示逻辑 | 业务规则、直接访问数据库 |
| 应用 | `src/app/` | 用例编排、事务边界 | 领域规则的具体实现 |
| 领域 | `src/domain/` | 实体、领域规则（纯逻辑，零框架依赖） | import 任何 UI/HTTP/DB/框架 |
| 基础设施 | `src/infra/` | DB、HTTP、外部服务适配 | 被领域层 import |

## 依赖矩阵

<!-- 谁可以 import 谁：✓ 允许，✗ 禁止，△ 仅通过接口/入口文件 -->

| 从 \ 到 | ui | app | domain | infra |
|---|---|---|---|---|
| ui      | —  | ✓ | ✓ | △ |
| app     | ✗ | — | ✓ | ✓ |
| domain  | ✗ | ✗ | — | ✗ |
| infra   | ✗ | △ | ✓ | — |

## 模块边界

- 跨模块引用必须走公共入口（`index.ts` / `__init__.py` / `mod.ts`）。
- `*/internal/`、`*/impl/` 下的内容禁止被本模块之外 import。
- 公共 API 不得返回内部类型或 ORM 对象。

## 长耗时任务与状态同步

<!-- 按项目实际的状态机与消费方式修改；arch-check 的"状态同步专项"按本节执行 -->

- **状态机**：长耗时任务统一 `pending → running → completed / failed`；
  下游**只消费终态**，`in_progress / partial` 等中间态对下游不可见。
- **完成信号显式且原子**：全部数据与终态（`status='completed'`）在同一事务落库；
  禁止"数据出现 = 步骤完成"的隐式判断，禁止先插数据、另补状态的两步式写入。
- **多实例消费**：多个实例同时监听同一状态时，必须原子认领
  （`UPDATE … SET status='running', owner=? WHERE … AND status='ready'`，
  检查影响行数）或消费幂等（业务唯一键去重）。禁止 check-then-act。
- **完成事件**：在数据库事务提交后发布（或 outbox 模式），禁止事务内直接发布。
- 至少一次投递的队列必须配幂等消费；可能乱序时用版本号护栏。

## 日志与链路追踪

<!-- 按项目实际的日志框架与命名修改；arch-check 的"日志串联专项"按本节执行 -->

- **ID 串联**：在线流程的日志必须能串起来、能标识来源——
  请求链路带 `request_id`；涉及用户带 `user_id`；
  涉及企业/多租户带 `tenant_id`（本项目统一命名：`tenant_id`）。
- **入口注入**：网关/中间件生成或透传 `X-Request-Id`，与 user_id/tenant_id
  一起一次性注入日志上下文（MDC / AsyncLocalStorage / logger context），
  全链路自动携带；禁止靠方法参数层层手工传递。
- **异步边界**：队列消息、定时任务、线程切换处把 `_ctx`（request_id/user_id/
  tenant_id）随载荷传递，消费侧恢复上下文；禁止异步任务重新生成新 ID 断链。
- **出站调用**：跨服务 HTTP 调用透传 `X-Request-Id` header。
- **安全**：禁止把 token/密码/密钥写入日志；ID 可以打，凭证不能打。

## 数据库访问

<!-- 按项目选定的 ORM 修改；arch-check 的"数据库访问专项"按本节执行 -->

- **ORM**：本项目使用 `<SQLAlchemy / Prisma / TypeORM / MyBatis / JPA / GORM>`；
  建表/改表一律走 migration，增删改查等简单操作走 ORM 模型。
- **原生 SQL**：仅限 ORM 表达不了的复杂查询（多表 JOIN、窗口函数、CTE、
  批量优化），集中在 `infra/dao` 层，使用前注明理由。
- **显式字段**：原生 SQL 禁止 `SELECT *` 与 `table.*`，必须列出具体字段；
  ORM 大表查询按需 `select` 字段，避免全字段拉取。
- **写安全**：禁止无 WHERE 的 UPDATE/DELETE；多语句数据变更包在事务里
  （数据与状态终态同一事务，见"长耗时任务与状态同步"）。

## 批量数据同步

<!-- 按项目实际的同步场景修改；arch-check 的"批量数据同步专项"按本节执行 -->

- **禁止循环逐条写入**：数据导入/同步/迁移场景一律分批批量写入
  （ORM `bulk_create`/`createMany`/`insertMany`；原生 SQL 多行 INSERT 或
  `COPY`/`LOAD DATA`/`executemany`），禁止 `for item in data: db.insert(item)`。
- **batch size 可配置**：默认 `BATCH_SIZE = 500`（单行约 200B → 每批 ≈ 100KB，
  远低于 MySQL `max_allowed_packet` 默认值）。生产可按实测调优，但必须保持
  为常量且有注释说明选择依据。
- **事务粒度**：每批一个事务（非全量一个超大事务），某批失败记录日志/重试。
- **完成信号**：同步完成后置显式终态（见"长耗时任务与状态同步"），
  禁止下游以"数据出现"判断同步完毕。

## 长耗时任务超时设计

<!-- 按项目实际的任务框架与时间配置修改；arch-check 的"任务超时设计专项"按本节执行 -->

- **双窗口超时（必须同时设置）**：
  - `TASK_ABSOLUTE_TIMEOUT`（默认 `7200s`）：任务从启动到终止的绝对上限。
  - `TASK_PROGRESS_TIMEOUT`（默认 `300s`）：保活窗口，在总超时到期时，
    若最近进度刷新距今 < 此值，继续等待；否则置为超时失败。
  - 约束：`TASK_PROGRESS_TIMEOUT < TASK_ABSOLUTE_TIMEOUT`（否则保活永不触发）。
- **超时判断逻辑（双条件 AND）**：
  ```
  总超时到期 = now > started_at + ABSOLUTE_TIMEOUT
  保活窗口超时 = now > last_progress_at + PROGRESS_TIMEOUT
  超时失败 ← 总超时到期 AND 保活窗口超时
  ```
- **进度刷新**：每完成一个可见进度单元（一批数据、一个阶段），
  同时更新 `last_progress_at`（UTC）和 `processed_count`（进度计数），
  禁止纯心跳（只刷时间不更新计数——卡死也续命）。
- **超时终态**：置为 `timeout` 并记录原因（区分"总超时+无进度"与"总超时+有进度仍等待"）；
  超时任务不得停留在 `running`，下游消费方只消费终态。
- **时钟统一**：所有时间字段和比较统一使用 UTC；worker 与 scheduler 依赖同一时钟源。
- **配置来源**：超时阈值从环境变量/配置中心读取，不硬编码。

## 外部服务调用追踪

<!-- 按项目实际的外部服务清单修改；arch-check 的"外部调用追踪专项"按本节执行 -->

- **响应 ID 必须入日志**：调用外部服务（大模型 API、业务 API、第三方服务）后，
  日志必须记录对方返回的唯一 ID（响应 header `X-Request-Id`/`X-Trace-Id`，
  或响应体 `id`/`request_id`/`completion_id`/`transaction_id`）。
- **与本地 request_id 关联**：同一条日志里同时携带本地 `request_id` 和外部
  `external_id`，实现双向溯源。如果已有日志上下文注入，`request_id` 自动携带。
- **异常路径也记录**：4xx/5xx/超时时仍尝试记录对方返回的 id 和 error 摘要。
- **大模型调用额外记录**：`model`（实际使用的模型名）、`usage`（token 用量），
  便于成本追踪。
- **外部服务清单**（维护点——按项目实际填写）：

| 服务 | 典型调用方式 | 响应 ID 字段 |
|---|---|---|
| OpenAI / Azure OpenAI | `openai.chat.completions.create()` | `response.id` |
| Claude (Anthropic) | `anthropic.messages.create()` | `response.id` |
| 支付网关 | `POST /charge` | `response.data.transaction_id` |
| 内部用户服务 | `GET /users/:id` | `response.headers['x-request-id']` |

## 高频写入表

<!-- 按项目实际的高频记录表修改；arch-check 的"高频写入表专项"按本节执行 -->

- **写入不阻塞主流程**：高频记录表（任务执行记录、行为日志、操作流水、
  审计日志等）的写入必须脱离请求主路径——通过 buffer flush（内存攒批 +
  定时/定量刷盘）或异步消息队列解耦。
- **事务隔离**：记录写入与主业务数据**不在同一事务**——记录写入失败
  不得导致主业务事务回滚。
- **分表/分区存储**：此类表数据量随时间线性增长，必须按时间或业务字段
  分表/分区（`PARTITION BY RANGE(TO_DAYS(created_at))`），按月/按周分区，
  冷数据定期归档。查询侧必须带分区键。
- **buffer 容错**：
  - flush 失败有重试策略（指数退避）；
  - 进程退出前注册 shutdown hook 做最终 drain；
  - 如使用纯内存 buffer，需评估进程崩溃丢数据的容忍度。
- **连接池隔离**：记录表写入使用独立连接池/数据源，不与主业务共享。
- **高频写入表清单**（维护点——按项目实际填写）：

| 表名 | 写入频率 | 缓冲方式 | 分区策略 |
|---|---|---|---|
| `task_records` | 每个任务执行写一条 | buffer flush (500条/5s) | 按月分区 |
| `operation_logs` | 每次 API 调用写一条 | Kafka → 批量消费 | 按天分区 |
| `behavior_events` | 前端埋点上报 | Redis Stream → 消费者 | 按周分区 |

## 时间字段类型

<!-- 按项目实际的数据库和大数据平台修改；arch-check 的"时间字段类型专项"按本节执行 -->

- **统一使用 `TIMESTAMP`**：数据表的所有时间字段（created_at、updated_at、
  expired_at、started_at 等）一律使用 `TIMESTAMP` 类型，禁止 `DATETIME`/`DATE`。
- **原因**：业务数据会流入大数据平台（Hive / Spark / Presto 等）做分析，
  `TIMESTAMP` 可直接映射为 Hive `TIMESTAMP` 并参与时间运算，
  `DATETIME`/`DATE` 只能映射为 `STRING`，无法直接做时间比较、区间过滤、分区裁剪。
- **ORM 映射**：确保 ORM 模型的时间字段映射到数据库时生成 `TIMESTAMP`（非 `DATETIME`）。
  - SQLAlchemy: `Column(TIMESTAMP, ...)`
  - TypeORM: `@Column({ type: 'timestamp' })`
  - Django: 需确认 migration 输出（默认 `DateTimeField` 映射 `DATETIME`）
  - Prisma: `@db.Timestamp` 指令
- **时区处理**：统一 UTC 存储；MySQL `TIMESTAMP` 自动按连接时区转换，
  应用连接统一设置 `SET time_zone = '+00:00'`；PostgreSQL 推荐 `TIMESTAMPTZ`。
- **合理例外**：MySQL `TIMESTAMP` 范围 1970~2038——超出范围的场景（如生日、
  历史日期）可用 `DATETIME`，但需标注 `arch-debt:` 并在大数据侧做 CAST 适配。

## 架构债约定

- 有意保留的架构妥协在代码处标记：`// arch-debt: <上限/风险>, <升级触发条件>, <关联>`
- `/arch-debt` 定期收割台账，到期项进入修复计划。

## 变更流程

涉及分层/新增模块的改动：完成后跑 `/arch-check`；大重构前先跑 `/arch-audit` 摸底。
涉及异步任务、状态字段、消息订阅的改动：完成后重点看 /arch-check 报告中的
"状态同步专项"部分。
涉及日志、请求入口、跨服务调用、队列的改动：完成后重点看 /arch-check 报告中的
"日志串联专项"部分。
涉及数据库访问、模型、migration、原生 SQL 的改动：完成后重点看 /arch-check
报告中的"数据库访问专项"部分。
涉及数据导入/同步/迁移、批量写入的改动：完成后重点看 /arch-check 报告中的
"批量数据同步专项"部分。
涉及长耗时任务、job/worker、超时逻辑、进度刷新的改动：完成后重点看 /arch-check
报告中的"任务超时设计专项"部分。
涉及外部服务调用（HTTP 客户端、大模型 SDK、第三方 API）的改动：完成后重点看
/arch-check 报告中的"外部调用追踪专项"部分。
涉及高频写入表（记录表、日志表、流水表）的写入逻辑、buffer/队列机制、
分表/分区策略的改动：完成后重点看 /arch-check 报告中的"高频写入表专项"部分。
涉及数据库建表（CREATE TABLE / ALTER TABLE）、ORM 模型定义、migration 中的
时间字段类型选择：完成后重点看 /arch-check 报告中的"时间字段类型专项"部分。
