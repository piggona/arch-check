# arch-check 架构检查规则（压缩版）

写代码前读一眼，改完代码跑 /arch-check。只管架构，不管风格。

## 规则来源

项目根 `ARCHITECTURE.md`（分层定义 + 依赖矩阵）是最高契约。
没有契约时用下面的通用原则，并主动提议生成一份。

## 通用原则

1. **依赖只能向下/向内**：UI → 服务/应用层 → 领域层 ← 基础设施层。
   领域层（domain/entities/core）不得 import UI、HTTP、数据库、框架代码。
2. **无循环依赖**：模块级 A→B→A 是 blocker，无论分层。
3. **走公共接口**：不 import 其他模块的 `/internal/`、`/impl/` 或绕过入口文件直达深层。
4. **边界不泄漏**：公共 API 不返回内部类型或 ORM 对象。
5. **不过度设计**：单实现接口、无人调用的抽象层、纯转发 wrapper 同样是架构问题。
6. **长耗时任务状态同步**：步骤完成必须用显式信号（终态字段或事务提交后的事件）。
   "数据库里出现某数据" ≠ "步骤执行完了"——数据可能没同步完全（半插入、事务未提交、
   关联表未写完）。多实例同时监听状态时必须原子认领
   （`UPDATE…WHERE status='ready'` 且检查影响行数）或消费幂等；
   先查后改（check-then-act）是竞态根源。
7. **日志串联与来源标识**：在线流程的日志通过 ID 串联——请求链路带
   `request_id`，涉及用户带 `user_id`，涉及企业带 `tenant_id`/`enterprise_id`。
   入口中间件一次性注入日志上下文（MDC/AsyncLocalStorage），全链路自动携带；
   异步边界（队列/定时任务/线程切换）把 ID 随载荷传递并在消费侧恢复，
   禁止层层手工传参、禁止异步任务重新生成新 ID。
8. **数据库访问**：建表与增删改查等简单操作一律走 ORM（migration + 模型）；
   确需原生 SQL 的复杂查询（JOIN/窗口/CTE/批量优化）集中在 infra/DAO 层，
   **显式列出字段，禁止 `SELECT *`（含 `table.*`）**；
   ORM 大表查询按需 select 字段；无 WHERE 的 UPDATE/DELETE 是事故级违规。
9. **批量数据同步**：同步/导入数据到数据库时**禁止循环逐条插入**——
   N 条数据 = N 次数据库往返，数据量一大即耗时爆炸、连接池耗尽。
   必须分批（batch）批量写入（`bulk_create`/`createMany`/多行 INSERT/`COPY`），
   每批大小可配置且不超过 SQL 传输限制（MySQL `max_allowed_packet`、
   网络 buffer 等）；每批一个事务，某批失败有重试/记录策略。
10. **长耗时任务超时设计**：持续执行且有可查询进度的任务必须同时设置两个超时：
    **总超时（absolute_timeout）**——任务从启动到终止的绝对上限，防止无限运行；
    **进度保活超时（progress_timeout）**——总超时到期时，若最近进度刷新在
    保活窗口内，继续等待；保活窗口内也无进度时才置为超时失败。
    `progress_timeout < absolute_timeout`；进度刷新必须同时更新时间戳和进度计数
    （纯心跳无法区分卡死与推进）；超时阈值从配置读；时间比较统一 UTC。
11. **外部调用日志追踪**：调用外部服务（大模型 API、业务 API、第三方服务）后，
    日志必须记录对方返回的唯一 ID（response_id/completion_id/transaction_id 等），
    并与本地 request_id 关联——排查时需要拿对方的 id 去对方系统查。
    异常路径也要尝试记录对方的 id。大模型调用建议额外记 model 和 token usage。
12. **跟随现状**：新代码与既有分层结构冲突时，先质疑新代码。

## 检查输出

`<file>:<line> [BLOCKER|WARN|NOTE] <规则> — <问题> → <最小修复建议>`

- BLOCKER：方向违规、循环依赖、边界泄漏、隐式完成信号、无保护的共享消费、
  请求路径日志无 request_id、无 WHERE 的 UPDATE/DELETE、循环逐条写入数据库、
  长耗时任务缺总超时或缺进度保活超时、超时判断无保活条件或时钟不一致、
  核心外部服务调用日志无响应 ID 且无本地 request_id 关联，
  不合不能合入
- WARN：直达内部、过度抽象、与现状不一致、暴露中间态、事务外发事件、
  涉用户日志缺 user_id、多租户日志缺 tenant_id、异步边界上下文丢失、
  简单操作绕过 ORM、原生 SQL 用 SELECT *、批量写入未控制 batch size、
  超时阈值硬编码、进度刷新只更新时间戳不更新进度计数、
  外部服务调用日志缺对方响应 ID，可带标记合入
- NOTE：记录在案，不要求本次处理

暂缓修复的 WARN 在代码处标记：

```
// arch-debt: <上限/风险>, <升级触发条件>, <关联/日期>
```

`/arch-debt` 收割全部标记生成台账；缺触发条件的标记烂掉风险最高。

## 命令

`/arch-check [staged|branch|<range>]` 变更级检查 · `/arch-audit` 全库审计 ·
`/arch-debt` 债务台账 · `/arch-help` 速查

（本文件同样约束参与 arch-check 仓库开发的代理。尤其是他们。）
